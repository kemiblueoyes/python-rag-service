extends: existence
message: 'Punctuation should be inside the quotes.'
level: error
# Backticks mark inline code, not quotation marks. Leave those spans alone.
scope: ~code
nonword: true
tokens:
  - '["“][^"”“]+["”][.,]'
