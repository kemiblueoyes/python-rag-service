# Vale intentional violations

This file intentionally violates the RAGService Vale rules. Do not use it as documentation.

The RAG service exposes retrieval and answer-generation capabilities.

RRF combines ranked retrieval results.

The service performs re-ranking after the initial retrieval stage.

The RAG engine retrieves documentation from the index.

The AI knows whether the documentation contains an answer.

The model verifies its sources before returning an answer.

The model always answers from the supplied documentation.

Hybrid retrieval guarantees relevant results.

Citation validation prevents hallucinations.

The AI engine performs AI search over the documentation.

## Details

See here for more information.

As described above, the service retrieves candidate chunks.

In the previous section, the retrieval pipeline selected the results.
