# GENERATED FILE — DO NOT EDIT DIRECTLY.
# Source: FastAPI application schema.
# Regenerate with:
#   uv run python -m rag_service.commands.generate_openapi
# To check if fern/openapi.yml matches the current FastAPI schema.:
#   uv run python -m rag_service.commands.generate_openapi --check

openapi: 3.1.0
info:
  title: Python RAG Service
  version: 0.1.0
paths:
  /v1/search:
    post:
      tags:
      - Search
      summary: Search indexed documentation
      description: Retrieve documentation chunks through the hybrid retrieval pipeline
        without generating an answer.
      operationId: search_v1_search_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SearchRequest'
            examples:
              results_found:
                summary: Results found
                value:
                  query: How does metadata improve retrieval?
                  filters:
                    content_type: page
                  limit: 3
              no_results:
                summary: No relevant results
                value:
                  query: What is the capital of Mars?
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SearchResponse'
              examples:
                results_found:
                  summary: Results found
                  value:
                    query: How does metadata improve retrieval?
                    results:
                    - chunk_id: wordpress:page:1:chunk:0
                      document_id: wordpress:page:1
                      title: Metadata Strategy
                      heading_path:
                      - Metadata filtering
                      anchor: metadata-filtering
                      excerpt: Metadata can narrow the documents considered during
                        retrieval.
                      url: https://example.test/metadata
                      score: 0.91
                no_results:
                  summary: No relevant results
                  value:
                    query: What is the capital of Mars?
                    results: []
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              examples:
                authentication_failed:
                  summary: Authentication failed
                  value:
                    error:
                      code: authentication_failed
                      message: A valid API key is required.
                      details: []
        '422':
          description: Request validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              examples:
                validation_error:
                  summary: Validation error
                  value:
                    error:
                      code: validation_error
                      message: Request validation failed.
                      details:
                      - field: limit
                        message: Input should be greater than or equal to 1
        '503':
          description: Search is temporarily unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              examples:
                search_unavailable:
                  summary: Search unavailable
                  value:
                    error:
                      code: retrieval_unavailable
                      message: Search is temporarily unavailable.
                      details: []
      security:
      - APIKeyHeader: []
  /v1/answer:
    post:
      tags:
      - Answer
      summary: Generate a grounded answer
      description: Retrieve relevant documentation and generate an answer grounded
        in validated source content.
      operationId: answer_v1_answer_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AnswerRequest'
            examples:
              grounded_answer:
                summary: Grounded answer
                value:
                  query: Why does inconsistent terminology cause retrieval failures?
                  filters:
                    source: wordpress
              insufficient_evidence:
                summary: Insufficient evidence
                value:
                  query: What is the capital of Mars?
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AnswerResponse'
              examples:
                grounded_answer:
                  summary: Grounded answer
                  value:
                    query: Why does inconsistent terminology cause retrieval failures?
                    answer: Inconsistent terminology can cause retrieval failures
                      because the query and documentation may use different language
                      for the same concept. [S1]
                    sources:
                    - citation_id: S1
                      chunk_id: wordpress:page:1:chunk:0
                      document_id: wordpress:page:1
                      title: Retrieval Failures
                      heading_path:
                      - Vocabulary mismatch
                      anchor: vocabulary-mismatch
                      excerpt: Inconsistent terminology can make relevant content
                        harder to retrieve.
                      url: https://example.test/retrieval-failures
                    sufficient_evidence: true
                insufficient_evidence:
                  summary: Insufficient evidence
                  value:
                    query: What is the capital of Mars?
                    answer: The available documentation does not provide enough information
                      to answer this question.
                    sources: []
                    sufficient_evidence: false
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              examples:
                authentication_failed:
                  summary: Authentication failed
                  value:
                    error:
                      code: authentication_failed
                      message: A valid API key is required.
                      details: []
        '422':
          description: Request validation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              examples:
                validation_error:
                  summary: Validation error
                  value:
                    error:
                      code: validation_error
                      message: Request validation failed.
                      details:
                      - field: limit
                        message: Extra inputs are not permitted
        '503':
          description: Answer generation is temporarily unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              examples:
                answer_unavailable:
                  summary: Answer unavailable
                  value:
                    error:
                      code: answer_unavailable
                      message: Answer generation is temporarily unavailable.
                      details: []
      security:
      - APIKeyHeader: []
  /health:
    get:
      summary: Check service health
      description: Check whether the Python RAG Service is running and responding
        to requests.
      operationId: health_check_health_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
components:
  schemas:
    AnswerRequest:
      properties:
        query:
          type: string
          minLength: 1
          title: Query
          description: Natural-language question to answer.
          examples:
          - Why does inconsistent terminology cause retrieval failures?
        filters:
          anyOf:
          - $ref: '#/components/schemas/SearchFilters'
          - type: 'null'
          description: Optional metadata filters applied during retrieval.
      additionalProperties: false
      type: object
      required:
      - query
      title: AnswerRequest
      description: Request body for POST /v1/answer.
    AnswerResponse:
      properties:
        query:
          type: string
          title: Query
          description: The question submitted by the client.
        answer:
          type: string
          title: Answer
          description: Grounded answer generated from retrieved evidence.
        sources:
          items:
            $ref: '#/components/schemas/AnswerSource'
          type: array
          title: Sources
          description: Validated sources cited by the generated answer.
        sufficient_evidence:
          type: boolean
          title: Sufficient Evidence
          description: Whether the retrieved sources contained enough evidence to
            answer the question.
      type: object
      required:
      - query
      - answer
      - sources
      - sufficient_evidence
      title: AnswerResponse
      description: Successful response from POST /v1/answer.
    AnswerSource:
      properties:
        citation_id:
          type: string
          title: Citation Id
          description: Request-local citation identifier used in the generated answer.
          examples:
          - S1
        chunk_id:
          type: string
          title: Chunk Id
          description: Stable identifier for the supporting chunk.
        document_id:
          type: string
          title: Document Id
          description: Stable identifier for the source document.
        title:
          type: string
          title: Title
          description: Title of the source document.
        heading_path:
          items:
            type: string
          type: array
          title: Heading Path
          description: Heading hierarchy containing the supporting chunk.
        anchor:
          anyOf:
          - type: string
          - type: 'null'
          title: Anchor
          description: Source anchor for the supporting chunk's current heading.
        excerpt:
          type: string
          title: Excerpt
          description: Text from the supporting chunk.
        url:
          type: string
          title: Url
          description: Trusted URL for the source document.
      type: object
      required:
      - citation_id
      - chunk_id
      - document_id
      - title
      - heading_path
      - excerpt
      - url
      title: AnswerSource
      description: One validated source supporting a generated answer.
    ErrorBody:
      properties:
        code:
          type: string
          title: Code
          description: Stable machine-readable error code.
          examples:
          - validation_error
        message:
          type: string
          title: Message
          description: Human-readable summary of the error.
        details:
          items:
            $ref: '#/components/schemas/ErrorDetail'
          type: array
          title: Details
          description: Additional error details.
      type: object
      required:
      - code
      - message
      title: ErrorBody
    ErrorDetail:
      properties:
        field:
          anyOf:
          - type: string
          - type: 'null'
          title: Field
          description: Request field associated with the error.
          examples:
          - filters.site_id
        message:
          type: string
          title: Message
          description: Human-readable description of the error.
      type: object
      required:
      - message
      title: ErrorDetail
      description: Details about one request error.
    ErrorResponse:
      properties:
        error:
          $ref: '#/components/schemas/ErrorBody'
      type: object
      required:
      - error
      title: ErrorResponse
      description: Standard error response returned by the public API.
    HealthResponse:
      properties:
        status:
          type: string
          title: Status
          description: Current service health status.
          examples:
          - ok
      type: object
      required:
      - status
      title: HealthResponse
      description: Successful response from GET /health.
    SearchFilters:
      properties:
        document_id:
          anyOf:
          - type: string
            minLength: 1
          - items:
              type: string
              minLength: 1
            type: array
          - type: 'null'
          title: Document Id
          description: Limit results to one or more canonical document IDs.
        source:
          anyOf:
          - type: string
            minLength: 1
          - items:
              type: string
              minLength: 1
            type: array
          - type: 'null'
          title: Source
          description: Limit results to one or more content sources.
        source_id:
          anyOf:
          - type: string
            minLength: 1
          - items:
              type: string
              minLength: 1
            type: array
          - type: 'null'
          title: Source Id
          description: Limit results to one or more source-system document IDs.
        content_type:
          anyOf:
          - type: string
            minLength: 1
          - items:
              type: string
              minLength: 1
            type: array
          - type: 'null'
          title: Content Type
          description: Limit results to one or more content types.
      additionalProperties: false
      type: object
      title: SearchFilters
      description: Supported metadata filters for hybrid search.
    SearchRequest:
      properties:
        query:
          type: string
          minLength: 1
          title: Query
          description: Natural-language search query.
          examples:
          - How does metadata improve retrieval?
        filters:
          anyOf:
          - $ref: '#/components/schemas/SearchFilters'
          - type: 'null'
          description: Optional metadata filters applied during retrieval.
        limit:
          type: integer
          minimum: 1.0
          title: Limit
          description: Maximum number of search results to retrieve.
          default: 5
          examples:
          - 5
      additionalProperties: false
      type: object
      required:
      - query
      title: SearchRequest
      description: Request body for POST /v1/search.
    SearchResponse:
      properties:
        query:
          type: string
          title: Query
          description: The search query submitted by the client.
        results:
          items:
            $ref: '#/components/schemas/SearchResult'
          type: array
          title: Results
          description: Ranked search results that met the retrieval threshold.
      type: object
      required:
      - query
      - results
      title: SearchResponse
      description: Successful response from POST /v1/search.
    SearchResult:
      properties:
        chunk_id:
          type: string
          title: Chunk Id
          description: Stable identifier for the retrieved chunk.
        document_id:
          type: string
          title: Document Id
          description: Stable identifier for the source document.
        title:
          type: string
          title: Title
          description: Title of the source document.
        heading_path:
          items:
            type: string
          type: array
          title: Heading Path
          description: Heading hierarchy containing the retrieved chunk.
        anchor:
          anyOf:
          - type: string
          - type: 'null'
          title: Anchor
          description: Source anchor for the retrieved chunk's current heading.
        excerpt:
          type: string
          title: Excerpt
          description: Text from the retrieved chunk.
        url:
          type: string
          title: Url
          description: Trusted URL for the source document.
        score:
          type: number
          title: Score
          description: Rerank score for the result.
      type: object
      required:
      - chunk_id
      - document_id
      - title
      - heading_path
      - excerpt
      - url
      - score
      title: SearchResult
      description: One ranked search result.
  securitySchemes:
    APIKeyHeader:
      type: apiKey
      in: header
      name: X-API-Key
